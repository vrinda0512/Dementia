/**
 * Reserved for quiet cover and paper sounds when approved audio assets are available.
 * The MVP deliberately makes no sound requests and works without audio.
 */
export class DiaryAudio {
  playCover() {}
  playPageTurn() {}
  dispose() {}
}
